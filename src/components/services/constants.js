const baseUrl = 'https://487fbcc7d9f6.ngrok.io/';
const endPoint = 'api/users';
const auth = 'api/authenticate';
const notes = 'api/notes/';
export const defaultURL = baseUrl + endPoint;
export const authenticateURL = baseUrl + auth;
export const noteURL = baseUrl + notes;
